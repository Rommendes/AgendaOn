# AgendaOn

Aplicação web (React + Vite + Tailwind) integrada ao Supabase (Auth + Database).

## Stack
- React + Vite
- Tailwind CSS
- Supabase (Auth / Database)
- Node.js + npm

## Licença
Projeto privado. Uso, cópia ou redistribuição não autorizados sem permissão.

---

## Como rodar o projeto (local)

### 1) Pré-requisitos
- Git
- Node.js (recomendado via NVM)

### 2) Clonar o repositório
```bash
git clone git@github.com:Rommendes/AgendaOn.git
cd AgendaOn

